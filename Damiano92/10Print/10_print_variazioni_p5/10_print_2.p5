


var c=0; 

function setup() {
  size(900, 900);
};

function draw() {
  background(#558899);
  if (mousePressed) {
    for (int y=0; y<height; y+=25) { 
      for (int x=0; x<width; x+=25) { 
        c=int(random(2))*25; 
        line(x+c, y, x+25-c, y+25);
      }
    }
  }
};
